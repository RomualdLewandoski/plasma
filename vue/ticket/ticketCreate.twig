<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-9 col-lg-12 col-xl-10">
            <div class="card shadow-lg o-hidden border-0 my-5">
                <div class="card-body p-0">
                    <div class="p-5">
                        <div class="text-center">
                            <h4 class="text-dark mb-4">{{ translate("CREATE_TICKET") }}

                            </h4>
                        </div>
                        {{ form.form_start("user") }}
                        {{ form.fill() }}
                        <button class="btn btn-primary d-block btn-user w-100" type="submit">
                            {{ translate("CREATE_TICKET_BTN") }}
                        </button>
                        <hr>
                        {{ form.form_end() }}

                        <script>
                            $('#targets').iv_selectConvert( {text_el_style:'width:80%!important;border: 1px solid black;'} );
                        </script>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
